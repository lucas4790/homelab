apiVersion: v1
kind: Service
metadata:
  name: {{ include "authentic.fullname" . }}
  labels:
    {{- include "authentic.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "authentic.selectorLabels" . | nindent 4 }}
